# Legacy frontend retired

The old `frontend/public/app.js`, `index.html`, `styles.css`, and `api.js` runtime files have been removed. The canonical frontend is the Vite application under `frontend/app-src/`. Production serves only the validated Vite `dist` build.
