# Historical Engineering Archive

All documents in this directory are retained for traceability only. They describe earlier repository states and must not be used as the current production-status source of truth. Current implementation and acceptance status live in the non-archive `docs/` directory.
